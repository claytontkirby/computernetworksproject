#!/bin/bash

x-terminal-emulator -e client.out rcv & wait

x-terminal-emulator -e client.out rcv & wait

x-terminal-emulator -e client.out rcv & wait

x-terminal-emulator -e client.out rcv & wait

x-terminal-emulator -e client.out rcv & wait
